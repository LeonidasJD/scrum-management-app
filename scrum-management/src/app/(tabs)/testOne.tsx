import { ThemedText, ThemedView } from "@/src/shared/components";

const TestOne = () => {
  return (
    <ThemedView>
      <ThemedText variant="bold" color="red" size="xxl">
        Test One
      </ThemedText>
    </ThemedView>
  );
};

export default TestOne;
